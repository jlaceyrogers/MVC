<section class="posts">
    {{#each posts}}
    <article class="post">
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <a href="/post/{{this.id}}">Read more</a>
    </article>
    {{/each}}
</section>
